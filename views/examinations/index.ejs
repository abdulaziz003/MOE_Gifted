
<h2>جميع الاختبارات</h2>

<br>

<a class="btn btn-success" href="/examinations/new">اضافة اختبار جديد</a>

<br>
<br>
<% if(exams.length > 0 ){ %>
  <table class="table table-striped table-sm">
    <thead>
      <tr>
        <th scope="col">اسم الاختبار</th>
        <th scope="col">الدرجة الاجمالية</th>
        <th scope="col">تاريخ الاختبار</th>
        <th scope="col">الاجراءات</th>
      </tr>
    </thead>
    <tbody>
      <% exams.forEach(exam => { %>
        <!-- On rows -->
        <tr>
          <td>
            <%= exam.name %>
          </td>
          <td>
            <%= exam.totalMark %>
          </td>
          <td>
            <% momentHijri.locale("en") %>
            <%let gda = momentHijri(exam.publishedAt).format("YYYY-MM-DD")%>
            <%= momentHijri(gda, " YYYY-M-D ").format("iYYYY/iM/iD ") %>
          </td>
          <td>
            <span>
              <a class="btn btn-sm btn-success" href="/examinations/<%=exam.id%>">عرض </a>
            </span>
            <span>
              <a class="btn btn-sm btn-info" href="/examinations/<%=exam.id%>/edit">تعديل </a>
            </span>
            <%- include('../partials/deleteForm', { url: `/examinations/${exam.id}` }) %>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>
  <% }else{ %>
    <h4>لايوجد اختبارات</h4>
    <% } %>