<div class="text-center">
  <h2>جميع البرامج الاثرائية</h2>
  <form action="/students" method="get">
    <label for="author-name">اسم البرنامج</label>
    <input type="text" name="name" id="author-name" value="<%= searchOptions.name%>">
    <button class="btn btn-sm btn-warning" type="submit">بحث</button>
  </form>
  <br>
  
  <a class="btn btn-success" href="/courses/new">اضافة برنامج جديد</a>
</div>

<br>
<br>
<% if(courses.length > 0 ){ %>
  <table class="table table-striped table-sm text-center">
    <thead>
      <tr>
        <th scope="col">اسم البرنامج</th>
        <th scope="col">تاريخ النشر</th>
        <th scope="col">مدة البرنامج</th>
        <th scope="col">الطلاب المسجلين</th>
        <th scope="col">الاجراءات</th>
      </tr>
    </thead>
    <tbody>
      <% courses.forEach(course => { %>
        <!-- On rows -->
        <tr>
          <td>
            <%= course.name %>
          </td>
          <td>
            <%= course.publishedAt %>
          </td>
          <td>
            <%= course.duration %>
          </td>
          <td>
            <%= course.location %>
          </td>
          <td>
            <%= course.students.length %>
          </td>
          <td>
            <span>
              <a style="text-decoration: none;" class="ml-2" href="/courses/<%=course.id%>">👁</a>
            </span>
            <span>
              <a style="text-decoration: none;" class="ml-2" href="/courses/<%=course.id%>/edit">✏️ </a>
            </span>
            <span>
              <a style="text-decoration: none;" class="ml-2" href="/courses/<%=course.id%>/register">👨🏻‍🎓</a>
            </span>
            <%- include('../partials/deleteForm', { url: `/courses/${course.id}` }) %>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>
  <% }else{ %>
    <h4 class="text-center">لايوجد برامج اثرائية</h4>
    <% } %>